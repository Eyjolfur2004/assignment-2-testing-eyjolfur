{
  "name": "assignment-2-testing",
  "version": "1.0.0",
  "dependencies": {
    "date-fns": "^4.1.0"
  },
  "engines": {
    "bun": ">=1.3.0"
  },
  "scripts": {
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "3.2.4",
    "vite": "4.5.3",
    "vitest": "3.2.4"
  }
}
